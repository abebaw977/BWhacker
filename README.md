# BWhacker

BWhacker is a tool/script collection related to hacking and penetration testing.

## Overview

This repository contains multiple scripts and modules organized into folders that include:

- `Hacker/BackDoor/` — Backdoor related scripts  
- `Hacker/` — Various hacking utilities like `BlackHat.py`, `DnsResolve.py`, `Banner.py`, etc.  
- `Hacker/dnscan` — (Submodule or tool included)  
- Additional scripts like `First.py` and others

## Installation

1. Clone the repo:
   ```bash
   git clone https://github.com/abebaw977/BWhacker.git
   cd BWhacker
